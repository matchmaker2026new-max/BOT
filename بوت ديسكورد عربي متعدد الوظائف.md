# بوت ديسكورد عربي متعدد الوظائف

بوت Discord.js v14 عربي، يدعم أوامر Slash الرسمية وأوامر Prefix عربية مختصرة.

## التشغيل

1. ثبّت Node.js 18.17 أو أحدث.
2. انسخ `.env.example` إلى `.env` وضع `DISCORD_TOKEN` و`CLIENT_ID`.
3. من Discord Developer Portal فعّل **Server Members Intent** و**Message Content Intent** عند الحاجة.
4. شغّل:

```bash
npm install
npm run deploy
npm start
```

ضع `GUILD_ID` أثناء التطوير لتسجيل الأوامر فورًا في سيرفر واحد. اتركه فارغًا لتسجيلها عالميًا؛ قد يستغرق ظهورها حتى ساعة.

## الاختصارات العربية

`م` مسح، `ب` باند، `ط` طرد، `ك` كتم، `فك` فك الكتم، `ق` قفل، `فتح` فتح، `معلومات` معلومات السيرفر، `صورة` أفاتار، `بانر` بانر، `تذكرة` إنشاء تذكرة، `سحب` بدء سحب.

كل أمر Slash من القائمة الأصلية موجود أيضًا باسمه الإنجليزي. بعض الأوامر الإدارية تتطلب صلاحية Discord المناسبة، والبوت لا يستطيع تجاوز صلاحيات مالكه أو صلاحيات الأدوار.

## ملاحظات مهمة

- لا تضع التوكن داخل Git أو ترسله لأي شخص.
- أوامر `anti-delete-roles` و`anti-delete-rooms` تحفظ Snapshot بسيطًا وتحاول إعادة العنصر المحذوف؛ يلزم للبوت صلاحيات Manage Roles/Channels.
- أوامر `gend` و`greroll` و`gstart` تنفذ نظام Giveaways داخل الذاكرة، وتُعاد تهيئتها عند إعادة تشغيل البوت.
- غيّر `PREFIX` من `src/config.js` إن أردت بادئة أخرى.
